var primary = localStorage.getItem("primary") || '#329C57';
var secondary = localStorage.getItem("secondary") || '#f73164';

window.cubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
